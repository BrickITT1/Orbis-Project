export interface fastUserInfo {
    id: number;
    name: string;
    avatar_url: string;
}

export interface UserInfo {
    id: number;
    name: string;
    avatar_url: string;
    about: string;
    gender: string;
}